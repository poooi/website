{
  "private": true,
  "name": "worker",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "dist/index.js",
  "author": "Ashley Lewis <ashleymichal@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "~0.2.0",
    "itty-router": "^2.6.1",
    "mime": "^3.0.0",
    "toucan-js": "^2.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.4.0",
    "@types/mime": "^2.0.3",
    "cross-env": "^7.0.3",
    "esbuild": "^0.14.27",
    "jest-environment-miniflare": "^2.5.0",
    "miniflare": "^2.3.0",
    "ts-jest": "^27.1.3"
  },
  "peerDependencies": {
    "@poi-web/data": "*"
  },
  "scripts": {
    "build": "esbuild index.ts --bundle --sourcemap=both --external:__STATIC_CONTENT_MANIFEST --format=esm --outfile=dist/index.js",
    "test": "yarn build & yarn cross-env NODE_OPTIONS=--experimental-vm-modules jest",
    "test:ci": "yarn build & yarn cross-env NODE_OPTIONS=--experimental-vm-modules jest --coverage --reporters=default --reporters=jest-junit --coverageReporters=cobertura --coverageReporters=lcov",
    "typecheck": "tsc --noEmit"
  }
}
